(roleattribute xserver_wrap_role)

(block xserver_wrap
	(blockinherit app.application_blk)

	(roletype xserver_wrap_role subj)

	(allow subj self (capability (setuid setgid sys_admin)))

	(call dev_rw_dri (subj))

	(call xserver.run (subj xserver_wrap_role))
	(call xserver.read_config (subj)))
